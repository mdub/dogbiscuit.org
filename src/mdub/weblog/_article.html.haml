---
layout: "../_layout.html.haml"
...

- @body_class = "article"

%h1= page.title

- if page.publication_date
  - content_for[:sidebar] += capture_haml do
    %p.publication_date
      Published: 
      = page.publication_date.strftime("%e %b, %Y")

~ yield
