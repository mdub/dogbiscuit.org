---
layout: "_layout.html.haml"
title: Archive
...

- content_for[:head] += capture_haml do
  %link{ :rel=>"alternate", :href=>href("feed.atom"), :type=>"application/atom+xml" }

%ul.articles
  - weblog_archive.each do |entry|
    %li
      %p
        = link(entry)
        %span.teaser
          %span.published_at
            on
            = entry.published_at.strftime("%e %b, %Y")
